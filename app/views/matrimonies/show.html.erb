
<div class="album text-muted">
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <%= image_tag @matrimonies.main_image_url, size: '650x350' %>
                    <h2> <%= @matrimonies.comments.count %> Comments </h2>
                    <%= render @matrimonies.comments %>
                    <h3>Add a Comments </h3>
                    <%= render 'comments/form' %>

                <br>
            </div>
            <div class="col-md-4">
                <h3>Name : <%= @matrimonies.name %></h3>
                <h3>Age : <%= @matrimonies.age %></h3>
                <h3>spa : <%= @matrimonies.spa %></h3>
                <br>
            </div>
         </div>
     </div>
 </div>